"""
Repositories for RomaSub.AI
Data access layer for database operations

Pure functional modules - import as:
    from repositories import user as user_repo

Then use:
    user_repo.get_user_by_email(db, email)
    user_repo.create_user(db, user_data)
"""

# No class exports - use module imports directly
# Example: from repositories import user
