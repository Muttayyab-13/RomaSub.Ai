# FastAPI and server
fastapi
uvicorn[standard]
python-multipart

# Database
sqlalchemy
psycopg2-binary
alembic

# Authentication
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# Google OAuth
google-auth
google-auth-oauthlib

# Email (MailerSend)
mailersend

# Whisper ASR (faster-whisper uses CTranslate2 — 4-8x faster on CPU than openai-whisper)
faster-whisper
torch

# Transliteration (M2M100)
transformers>=4.45.0
sentencepiece
safetensors
rapidfuzz
# Urdu normalization. We use the leaf import `urduhack.normalization.character`
# so TensorFlow is NOT required at runtime. If a TF-free install is needed:
# `pip install --no-deps urduhack regex` (regex is the only real dep).
urduhack>=1.0.3
# Claude API for post-m2m100 LLM refinement
anthropic>=0.40.0

# Media processing
ffmpeg-python
ffmpeg
pydub
static_ffmpeg

# Utilities
python-dotenv
pydantic
pydantic-settings

# Demo UI
streamlit
requests