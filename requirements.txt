# FastAPI and server
fastapi
uvicorn[standard]
python-multipart

# Database
sqlalchemy
psycopg2-binary
alembic

# Authentication
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# Google OAuth
google-auth
google-auth-oauthlib

# Email (Brevo transactional API via REST — uses `requests` listed below)

# Whisper ASR
# openai-whisper: local "openai" backend. faster-whisper: local "faster"
# backend (CTranslate2, int8 — lower RAM, ~4x faster on CPU). groq: cloud
# Whisper backend (whisper-large-v3-turbo) — see settings.whisper_backend.
openai-whisper
faster-whisper
groq
torch

# Transliteration (M2M100)
transformers>=4.45.0
sentencepiece
safetensors
rapidfuzz
# Urdu normalization. We use the leaf import `urduhack.normalization.character`
# so TensorFlow is NOT required at runtime. If a TF-free install is needed:
# `pip install --no-deps urduhack regex` (regex is the only real dep).
urduhack>=1.0.3
# Claude API for post-m2m100 LLM refinement
anthropic>=0.40.0

# Media processing
ffmpeg-python
ffmpeg
pydub
static_ffmpeg

# Utilities
python-dotenv
pydantic
pydantic-settings
pydantic[Email]

# Demo UI
streamlit
requests

# M2M100 evaluation scripts (scripts/run_raw_m2m100.py)
sacrebleu
jiwer
datasets

# Testing
pytest
